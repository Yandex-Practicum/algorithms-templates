func getExcessiveLetter(_ shorter: String, _ longer: String) -> Character{
    // Здесь реализация вашего решения
}

func main() {
    let shorter = readLine()!
    let longer = readLine()!
    print(getExcessiveLetter(shorter, longer))
}

main()
