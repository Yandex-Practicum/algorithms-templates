#include <cassert>
#include "solution.h"


int siftUp(std::vector<int>& heap, int idx) {
    // Your code
    // “ヽ(´▽｀)ノ”
}

void test() {
    std::vector<int> sample = {-1, 12, 6, 8, 3, 15, 7};
    assert(siftUp(sample, 5) == 1);
}
