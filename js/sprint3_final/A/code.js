function brokenSearch(arr, k) {
    // Your code
	// “ヽ(´▽｀)ノ”
}

function test() {
    const arr = [19, 21, 100, 101, 1, 4, 5, 7, 12];
    if (brokenSearch(arr, 5) !== 6)  {
        console.error("WA");
    }
}
